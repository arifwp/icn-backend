import { TaskRecommendationDto } from './task-recommendation.dto';

describe('TaskRecommendationDto', () => {
  it('should be defined', () => {
    expect(new TaskRecommendationDto()).toBeDefined();
  });
});
